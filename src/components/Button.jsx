import React, { useState } from "react";

const Button = () => {
  // const [initial, setInitial] = useState("Submit") 
  return (
    <button  type="submit" className={"btn"}> Submit
    </button>
  );
};

export default Button;